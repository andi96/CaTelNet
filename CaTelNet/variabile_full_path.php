<?php

if(!isset($full_path))
		$full_path = "C:/xampp/htdocs/CaTelNet/";  // full path-ul spre root pentru include-uri
	
if(!isset($full_path_utilitar))
		$full_path_utilitar = "C:/xampp/htdocs/catelnet_utilitar/";  // full path-ul spre utilitar pentru include-uri
	
if(!isset($full_path_href))
		$full_path_href = "http://localhost/CaTelNet/";  // full path-ul spre root pentru href si src
	
if(!isset($full_path_utilitar_href))
		$full_path_utilitar_href = "http://localhost/catelnet_utilitar/";  // full path-ul spre utilitar pentru href si src
	
	
?>